$errorRed = #ab0000;
$warningYellow = #d0ad3e;

.swal2-popup.westworld-sweetalert {
  border: 2px solid $boxBorderColor;
  background-color: $boxBackgroundColor;

  .swal2-title {
      @extend .title;
  }

  .swal2-content{
    margin-bottom: 1em;
    color: $white;
    font-family: $defaultFontFamily;
  }

  .swal2-actions:not(.swal2-loading) {
    button {
      @extend .button;
      border-right-color: $delosLightBlue !important;
      border-left-color: $delosLightBlue !important;
    }
  }

  .swal2-loading {
    button {
      border-right-color: $white !important;
      border-left-color: $white !important;
    }
  }

  .swal2-icon {
    &.swal2-error {
      border-color: $errorRed;
      .swal2-x-mark {
        span {
          background-color: $errorRed;
        }
      }
    }

    &.swal2-warning {
      border-color: $warningYellow;
      .swal2-icon-text{
        color: $warningYellow;
      }
    }
  }

  // .swal2-close {
  //   color: $white;

  //   &:hover {
  //     color: lighten($white, 50%);
  //   }
  // }
}